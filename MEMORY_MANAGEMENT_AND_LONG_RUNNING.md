# Memory 管理和长期运行指南

## 1. 对话 Memory 检查

### 当前 Memory 管理机制

#### ✅ Discussion History 自动限制

**位置**：`backend/src/agents/multi_analyst_system.py`

**机制**：
```python
MAX_DISCUSSION_HISTORY_ENTRIES = 20  # 最多保留20条记录（约5轮完整讨论）
discussion_history = []

# 每次添加后自动限制
discussion_history.append({...})
_limit_discussion_history(discussion_history, MAX_DISCUSSION_HISTORY_ENTRIES)
```

**功能**：
- ✅ 自动限制内存中的对话历史为 20 条
- ✅ 只保留最近的记录，删除旧的记录
- ✅ 每次交易循环结束后自动清理

#### ✅ 交易循环后的内存清理

**位置**：`backend/src/orchestrator/trading_cycle.py`

**机制**：
```python
# 交易循环结束后
import gc
gc.collect()  # 强制垃圾回收，释放未使用的内存
print("[MEMORY] Trading cycle completed, memory cleaned")
```

**功能**：
- ✅ 每次交易循环结束后自动执行垃圾回收
- ✅ 释放未使用的内存
- ✅ 清理临时数据

#### ✅ 持久化 Memory 文件

**文件位置**：
- `backend/data/logs/discussion_actions.jsonl` - 对话记录
- `backend/data/logs/memory/daily/YYYY-MM-DD.json` - 每日记忆
- `backend/data/logs/memory/weekly/` - 每周记忆
- `backend/data/logs/memory/monthly/` - 每月记忆

**说明**：
- 这些文件是持久化存储，不会自动删除
- 用于前端显示和历史记录
- 可以通过 `system_init` 清除

### 检查是否有旧的 Memory

**检查方法**：

1. **检查 discussion_actions.jsonl 文件大小**：
   ```bash
   # 查看文件行数
   Get-Content backend\data\logs\discussion_actions.jsonl | Measure-Object -Line
   ```

2. **检查 memory 文件**：
   ```bash
   # 查看每日记忆文件
   ls backend\data\logs\memory\daily\
   ```

3. **检查内存中的 discussion_history**：
   - 每次交易循环开始时，`discussion_history = []`（重新初始化）
   - 不会保留上一次循环的历史

### ✅ 确认：没有旧的 Memory

**原因**：
1. ✅ **每次交易循环重新初始化**：`discussion_history = []`
2. ✅ **自动限制长度**：最多 20 条记录
3. ✅ **自动清理**：交易循环结束后执行 `gc.collect()`
4. ✅ **不跨循环保留**：每次循环都是新的 `discussion_history`

## 2. 长期运行系统注意事项

### ✅ 系统会自动处理的事项

1. **Memory 管理**
   - ✅ 自动限制对话历史长度（20 条）
   - ✅ 自动垃圾回收（每次交易循环后）
   - ✅ 自动清理临时数据

2. **订单管理**
   - ✅ 自动检查订单成交
   - ✅ 自动更新 portfolio 状态
   - ✅ 自动记录交易历史

3. **数据刷新**
   - ✅ 前端每 30 秒自动刷新数据
   - ✅ 交易循环每小时执行一次（交易时段）
   - ✅ 隔日计划每小时检查一次（休市时段）

### ⚠️ 需要注意的事项

#### 1. 后端 API 服务器

**必须保持运行**：
- ✅ 后端 API 服务器必须一直运行
- ✅ 如果服务器崩溃，会自动重启（如果使用 `start_api_stable.ps1`）
- ⚠️ 如果手动启动，需要确保服务器不会停止

**建议**：
- 使用 `scripts/start_api_stable.ps1` 启动（自动重启）
- 或使用 `scripts/restart_api_fast.ps1` 快速重启

#### 2. 前端网页

**可以关闭**：
- ✅ **可以关闭网页**，后端会继续运行
- ✅ 关闭网页不会影响后端交易逻辑
- ✅ 重新打开网页可以查看最新状态

**说明**：
- 前端只是显示界面，不执行交易逻辑
- 所有交易逻辑都在后端运行
- 关闭网页后，后端仍会：
  - 执行交易循环
  - 检查订单成交
  - 规划隔日交易
  - 保存数据

#### 3. 系统资源

**内存使用**：
- ✅ 自动限制对话历史（20 条）
- ✅ 自动垃圾回收
- ⚠️ 长期运行可能累积 memory 文件（但不会影响性能）

**磁盘空间**：
- ⚠️ `discussion_actions.jsonl` 会持续增长
- ⚠️ `memory/daily/` 文件会每天新增
- ⚠️ 建议定期清理旧文件（或使用 `system_init`）

#### 4. 网络连接

**需要保持**：
- ✅ 后端需要网络连接（获取市场数据）
- ✅ yfinance 需要网络连接
- ⚠️ 如果网络中断，工具调用会失败

#### 5. 系统时间

**需要准确**：
- ✅ 系统时间必须准确（用于判断交易时段）
- ⚠️ 如果系统时间错误，可能影响交易逻辑

### 📋 长期运行检查清单

#### 每日检查

- [ ] 检查后端 API 是否运行（访问 `http://localhost:8000/api/status`）
- [ ] 检查是否有错误日志
- [ ] 检查订单是否正常成交
- [ ] 检查 portfolio 状态是否正常

#### 每周检查

- [ ] 检查 `discussion_actions.jsonl` 文件大小
- [ ] 检查 memory 文件数量
- [ ] 检查磁盘空间
- [ ] 考虑清理旧文件（如果需要）

#### 每月检查

- [ ] 检查系统性能
- [ ] 检查日志文件大小
- [ ] 考虑备份重要数据
- [ ] 考虑清理旧 memory 文件

## 3. 可以关掉网页吗？

### ✅ 可以关闭网页

**原因**：
1. **前端只是显示界面**
   - 前端不执行交易逻辑
   - 只负责显示数据和接收用户操作

2. **后端独立运行**
   - 所有交易逻辑在后端
   - 后端会继续执行：
     - 交易循环
     - 订单检查
     - 隔日规划
     - 数据保存

3. **重新打开网页**
   - 可以随时重新打开网页
   - 会显示最新的数据
   - 不会丢失任何数据

### 📊 关闭网页后的行为

**后端继续运行**：
- ✅ 每小时执行交易循环（交易时段）
- ✅ 每小时检查隔日计划（休市时段）
- ✅ 每 30 秒检查订单成交（交易时段）
- ✅ 保存所有数据到文件

**前端停止**：
- ❌ 停止数据刷新
- ❌ 停止显示更新
- ❌ 停止用户交互

**重新打开后**：
- ✅ 自动加载最新数据
- ✅ 恢复所有功能
- ✅ 显示所有历史记录

### 💡 建议

1. **保持后端运行**：
   - 使用 `start_api_stable.ps1` 启动后端
   - 确保后端不会停止

2. **可以关闭前端**：
   - 需要查看时再打开
   - 不影响后端运行

3. **定期检查**：
   - 每天检查一次后端状态
   - 查看是否有错误日志

## 4. Memory 清理建议

### 自动清理（已实现）

- ✅ 内存中的 `discussion_history` 自动限制为 20 条
- ✅ 每次交易循环后自动垃圾回收
- ✅ 每次交易循环重新初始化 `discussion_history`

### 手动清理（可选）

如果需要清理旧的 memory 文件：

1. **使用 system_init**：
   - 会清除所有 memory 文件
   - 会清除所有对话记录
   - 会重置 portfolio

2. **手动删除文件**：
   ```bash
   # 删除旧的 memory 文件（保留最近 30 天）
   # 注意：需要谨慎操作
   ```

3. **定期备份**：
   - 定期备份重要数据
   - 然后清理旧文件

## 5. 总结

### ✅ Memory 管理

- ✅ **没有旧的 memory**：每次循环重新初始化
- ✅ **自动限制长度**：最多 20 条记录
- ✅ **自动清理**：交易循环结束后垃圾回收

### ✅ 长期运行

- ✅ **后端必须运行**：使用 `start_api_stable.ps1`
- ✅ **前端可以关闭**：不影响后端运行
- ✅ **自动处理**：memory、订单、数据刷新都自动处理

### ⚠️ 注意事项

- ⚠️ **网络连接**：需要保持网络连接
- ⚠️ **系统时间**：需要准确
- ⚠️ **磁盘空间**：定期检查文件大小
- ⚠️ **错误日志**：定期检查是否有错误

